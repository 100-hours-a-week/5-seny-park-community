const express = require("express");
const usersRouter = express.Router();
const usersController = require("../controllers/users.controller");

// usersRouter.get("/", usersController.getAllUsers);

module.express = usersRouter;
